# Mock detector: hard
# Heavy general smearing (σ=0.3) + value cut on column '0' (≥ 0.5)

defaults:
  - compose

detectors:
  - _target_: jetprism.detectors.GeneralSmearing
    sigma: 0.3
  - _target_: jetprism.detectors.ValueCut
    column: '0'
    min_value: 0.5
    max_value: null
