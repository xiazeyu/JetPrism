# MC-POM detector: medium
# Medium momentum smearing (σ=0.5) + medium cos(θ) cut (0.8)

defaults:
  - compose

detectors:
  - _target_: jetprism.detectors.MomentumSmearing
    sigma: 0.5
  - _target_: jetprism.detectors.CosThetaCut
    threshold: 0.8
